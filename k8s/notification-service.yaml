apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: flashservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: flashservice/notification-service:latest
          ports:
            - containerPort: 80
          env:
            - name: KAFKA_BROKERS
              value: "kafka.flashservice.svc.cluster.local:9093"
            - name: KAFKA_TOPIC
              value: "notification"
            - name: KAFKA_CONSUMER_GROUP
              value: "notification-service"
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: flashservice
spec:
  selector:
    app: notification-service
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
